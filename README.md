# rag-website-chatbot
An intelligent chatbot designed to ingest a specific URL and recursively scrape relevant content from its linked pages. Using Retrieval-Augmented Generation (RAG), the bot processes this collected content to provide accurate, context-aware answers to user questions with minimal latency. 
